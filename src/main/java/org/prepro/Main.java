package org.prepro;

public class Main {
    public static void main(String[] args) {
        Grid grid = new Grid();

        grid.print();
        //System.out.println("La grille est " + (grid.isValid() ? "valide." : "invalide."));
    }
}